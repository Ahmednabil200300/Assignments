namespace assignment
{
    internal class Program
    {
        static void Main(string[] args)
        {

            // assignment 3 OOP
            // 1 and 2

            /*
             * 
             */


            //Employees Emp1 = new Employees(10, "ola", SecurityPrivileges.Developer , 3000 , new HireDate(12, 10 , 2024), Gender.F);


            //Console.WriteLine(Emp1);

            // 3 

            /* 3.Create an array of Employees with size three a DBA,
             * Guest and the third one is security officer who have full permissions. (Employee [] EmpArr;)
             * Implement All the Necessary Member Functions on the Class (Getters, Setters)
                Define all the Necessary Constructors for the Class
                Allow NO RUNTIME errors if the user inputs any data
                Write down all the necessary Properties (Instead of setters and getters)

             */

            //Employees[] employees = new Employees[3];
            //employees[0] = new Employees(1, "Ola" , SecurityPrivileges.DBA,3000, new HireDate(12,10, 2024) , Gender.F);
            //employees[1] = new Employees(2, "Mohammed", SecurityPrivileges.Guest, 1500, new HireDate(8, 2, 2023), Gender.M);
            //employees[2] = new Employees(3, "Nour", SecurityPrivileges.SecurityOfficer,2000, new HireDate(12, 3, 2024), Gender.M);

            //foreach (Employees emp in employees) {
            //    Console.WriteLine(emp);
            
            //}





        }
    }
}
